ANTHROPIC_API_KEY=sk-ant-api03-...
QE_PASSCODE=let-me-in

# Neon Postgres (auto-injected when you connect Neon in the Vercel dashboard).
# DATABASE_URL is what we read first; POSTGRES_URL also works.
# DATABASE_URL=postgres://user:pass@host/db?sslmode=require
